{
  "name": "redot-maintenance-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Redot Global maintenance tracker — React SPA backed by Supabase (auth + database)",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "postinstall": "npm --prefix frontend install"
  }
}
